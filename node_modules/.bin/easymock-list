#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../easy-mock-cli/bin/easymock-list" "$@"
  ret=$?
else 
  node  "$basedir/../easy-mock-cli/bin/easymock-list" "$@"
  ret=$?
fi
exit $ret
